machine:
  java:
    version: oraclejdk8
  python:
    version: 2.7.10
dependencies:
  pre:
    - pip install --user codecov
test:
  override:
  - ./gradlew test jacocoTestReport
  - ./gradlew install
  post:
  - codecov

